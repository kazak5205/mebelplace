FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY current-server/package*.json ./
RUN npm install --production

# Copy application code
COPY current-server/ ./

# Create uploads directory
RUN mkdir -p uploads

# Expose port
EXPOSE 3001

# Start the application
CMD ["npm", "start"]