FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# Skip Cypress binary download (not needed for dev)
ENV CYPRESS_INSTALL_BINARY=0
RUN npm ci

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]

