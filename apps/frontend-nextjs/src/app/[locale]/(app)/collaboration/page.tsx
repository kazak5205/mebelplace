/**
 * Collaboration Page - Collaboration tools with Glass design system
 */

'use client';

// Force dynamic rendering to avoid stale chunks
export const dynamic = 'force-dynamic';

import GlassCollaborationScreen from './GlassCollaborationScreen';

export default function CollaborationPage() {
  return <GlassCollaborationScreen />;
}
